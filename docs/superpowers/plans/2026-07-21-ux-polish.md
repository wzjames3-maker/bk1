# UX 打磨（Error Boundary + 表单校验提示）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 消除页面白屏风险（Error Boundary）并让用户清楚知道创建向导中按钮禁用的原因（表单校验提示）。

**架构：** Next.js App Router 原生 `error.tsx` 做页面级错误兜底；创建向导在「下一步」按钮旁显示红色提示文案，告知用户缺少什么。

**技术栈：** Next.js error.tsx（Client Component）、React state、shadcn/ui

---

## 文件结构

| 操作 | 路径 | 职责 |
|------|------|------|
| 创建 | `src/app/(app)/error.tsx` | 应用内页面级错误兜底 UI |
| 创建 | `src/app/error.tsx` | 根级错误兜底（登录页等） |
| 修改 | `src/components/create/create-wizard.tsx` | 添加校验提示文案逻辑 |

---

### 任务 1：应用级 Error Boundary

**文件：**
- 创建：`src/app/(app)/error.tsx`
- 创建：`src/app/error.tsx`

- [ ] **步骤 1：创建应用内错误兜底页**

`src/app/(app)/error.tsx`（必须是 Client Component）：

```tsx
'use client'

import { Button } from '@/components/ui/button'

export default function AppError({
  error,
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  return (
    <div className="flex min-h-[60vh] flex-col items-center justify-center gap-4 text-center">
      <span className="text-5xl">😵</span>
      <h2 className="text-xl font-semibold">页面出了点问题</h2>
      <p className="max-w-md text-sm text-muted-foreground">
        {error.message || '发生了未知错误，请重试。'}
      </p>
      <div className="flex gap-3">
        <Button onClick={reset}>重试</Button>
        <Button variant="outline" onClick={() => window.location.assign('/dashboard')}>
          返回工作台
        </Button>
      </div>
    </div>
  )
}
```

- [ ] **步骤 2：创建根级错误兜底页**

`src/app/error.tsx`：

```tsx
'use client'

import { Button } from '@/components/ui/button'

export default function RootError({
  error,
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center gap-4 text-center">
      <span className="text-5xl">😵</span>
      <h2 className="text-xl font-semibold">出了点问题</h2>
      <p className="max-w-md text-sm text-muted-foreground">
        {error.message || '发生了未知错误。'}
      </p>
      <div className="flex gap-3">
        <Button onClick={reset}>重试</Button>
        <Button variant="outline" onClick={() => window.location.assign('/')}>
          返回首页
        </Button>
      </div>
    </div>
  )
}
```

- [ ] **步骤 3：验证类型检查通过**

运行：`npx tsc --noEmit`
预期：无错误

- [ ] **步骤 4：Commit**

```bash
git add src/app/error.tsx src/app/\(app\)/error.tsx
git commit -m "feat: add error boundary pages for graceful error handling"
```

---

### 任务 2：创建向导表单校验提示

**文件：**
- 修改：`src/components/create/create-wizard.tsx`

- [ ] **步骤 1：在导航按钮区添加校验提示**

在 `create-wizard.tsx` 中，找到 `canNext` 函数之后，新增一个 `validationHint` 函数：

```typescript
const validationHint = (): string | null => {
  if (step === 0) {
    if (mode === 'script' && segments.length === 0) {
      return '请输入或上传脚本内容'
    }
    if (mode === 'ai' && !topic.trim()) {
      return '请输入播客话题'
    }
  }
  if (step === 1) {
    if (params.voice_ids.length === 0) {
      return '请为所有角色选择音色'
    }
    if (params.voice_ids.length < params.roles_count) {
      return `还需为 ${params.roles_count - params.voice_ids.length} 个角色选择音色`
    }
  }
  if (step === 2) {
    if (!estimate) return '正在计算费用...'
    if (balance < (estimate?.total || 0)) {
      return `余额不足（需 $${estimate.total.toFixed(4)}，当前 $${balance.toFixed(4)}）`
    }
  }
  return null
}
```

- [ ] **步骤 2：在导航按钮区渲染提示文案**

将导航按钮区的 JSX（`{/* 导航按钮 */}` 部分）替换为：

```tsx
{/* 导航按钮 */}
<div className="space-y-2">
  {validationHint() && (
    <p className="text-right text-sm text-destructive">{validationHint()}</p>
  )}
  <div className="flex justify-between">
    <Button variant="outline" onClick={() => setStep(s => s - 1)} disabled={step === 0}>
      上一步
    </Button>
    {step < 2 ? (
      <Button
        onClick={() => {
          if (step === 1) setEstimateLoading(true)
          setStep(s => s + 1)
        }}
        disabled={!canNext()}
      >
        下一步
      </Button>
    ) : (
      <Button
        onClick={handleSubmit}
        disabled={submitting || !estimate || balance < (estimate?.total || 0)}
      >
        {submitting ? '创建中...' : `确认生成（$${estimate?.total?.toFixed(4) || '...'}）`}
      </Button>
    )}
  </div>
</div>
```

- [ ] **步骤 3：验证类型检查通过**

运行：`npx tsc --noEmit`
预期：无错误

- [ ] **步骤 4：Commit**

```bash
git add src/components/create/create-wizard.tsx
git commit -m "feat(create): add validation hints explaining why next button is disabled"
```

---

### 任务 3：验证

- [ ] **步骤 1：启动 dev server 验证 Error Boundary**

访问 `http://localhost:3000/dashboard`，页面正常。
（Error Boundary 无法手动触发，但确保不影响正常渲染）

- [ ] **步骤 2：验证表单校验提示**

访问 `http://localhost:3000/create`：
- Step 0 不填话题 → 显示「请输入播客话题」
- 切换到脚本模式不输入 → 显示「请输入或上传脚本内容」
- Step 1 不选音色 → 显示「请为所有角色选择音色」

- [ ] **步骤 3：最终 Commit + Push**

```bash
git add -A
git commit -m "feat: error boundary + form validation hints (complete)"
git push origin main
```
